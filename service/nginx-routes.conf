# Auto-generated Flask route location blocks
# Total routes: 32
# Container logging: Uses stderr for CloudWatch integration

# Routes with allowed query parameters:
#   /callback: code, scope, state, session_state, error, error_description
#   /checkout/failed: ref
#   /checkout/success: session_id
#   /statement/<statement_id>: items_view, show_payments, download
#   /statements: view, sort, dir

# Routes with directive overrides:
#   /api/upload-statements/preflight: client_max_body_size=10m
#   /statement/<statement_id>: proxy_read_timeout=120s
#   /upload-statements: client_max_body_size=10m

location ~ "^/$" {
    limit_except GET HEAD { deny all; }
    # Public page - UTMs logged by CloudFront, drop query strings silently
    if ($args != "") {
        rewrite ^(.*)$ $1? last;
    }
    proxy_pass http://gunicorn;
}

location ~ "^/about$" {
    limit_except GET HEAD { deny all; }
    # Public page - UTMs logged by CloudFront, drop query strings silently
    if ($args != "") {
        rewrite ^(.*)$ $1? last;
    }
    proxy_pass http://gunicorn;
}

location ~ "^/api/banner/dismiss$" {
    limit_except POST { deny all; }
    # Public page - UTMs logged by CloudFront, drop query strings silently
    if ($args != "") {
        rewrite ^(.*)$ $1? last;
    }
    proxy_pass http://gunicorn;
}

location ~ "^/api/checkout/create$" {
    limit_except POST { deny all; }
    # Private route - no query strings allowed
    if ($args != "") {
        rewrite ^(.*)$ $1? last;
    }
    proxy_pass http://gunicorn;
}

location ~ "^/api/tenant-statuses$" {
    limit_except GET HEAD { deny all; }
    # Private route - no query strings allowed
    if ($args != "") {
        rewrite ^(.*)$ $1? last;
    }
    proxy_pass http://gunicorn;
}

location ~ "^/api/tenants/[^/]+/sync$" {
    limit_except POST { deny all; }
    # Private route - no query strings allowed
    if ($args != "") {
        rewrite ^(.*)$ $1? last;
    }
    proxy_pass http://gunicorn;
}

location ~ "^/api/upload-statements/preflight$" {
    client_max_body_size 10m;
    limit_except POST { deny all; }
    # Private route - no query strings allowed
    if ($args != "") {
        rewrite ^(.*)$ $1? last;
    }
    proxy_pass http://gunicorn;
}

location ~ "^/billing-details$" {
    limit_except GET HEAD { deny all; }
    # Private route - no query strings allowed
    if ($args != "") {
        rewrite ^(.*)$ $1? last;
    }
    proxy_pass http://gunicorn;
}

location ~ "^/buy-tokens$" {
    limit_except GET HEAD POST { deny all; }
    # Private route - no query strings allowed
    if ($args != "") {
        rewrite ^(.*)$ $1? last;
    }
    proxy_pass http://gunicorn;
}

location ~ "^/callback$" {
    limit_except GET HEAD { deny all; }
    # Public page - UTMs logged by CloudFront, drop query strings silently
    if ($args != "") {
        rewrite ^(.*)$ $1? last;
    }
    proxy_pass http://gunicorn;
}

location ~ "^/checkout/cancel$" {
    limit_except GET HEAD { deny all; }
    # Private route - no query strings allowed
    if ($args != "") {
        rewrite ^(.*)$ $1? last;
    }
    proxy_pass http://gunicorn;
}

location ~ "^/checkout/failed$" {
    limit_except GET HEAD { deny all; }
    # Allowed parameters: ref
    if ($args ~ ".+" ) {set $check_args 1;}
    if ($args !~ "^ref=[^&<>\x22\x27]*$") {
        set $check_args "${check_args}1";
    }
    if ($check_args = "11") {
        return 404;
    }
    proxy_pass http://gunicorn;
}

location ~ "^/checkout/success$" {
    limit_except GET HEAD { deny all; }
    # Allowed parameters: session_id
    if ($args ~ ".+" ) {set $check_args 1;}
    if ($args !~ "^session_id=[^&<>\x22\x27]*$") {
        set $check_args "${check_args}1";
    }
    if ($check_args = "11") {
        return 404;
    }
    proxy_pass http://gunicorn;
}

location ~ "^/cookies$" {
    limit_except GET HEAD { deny all; }
    # Public page - UTMs logged by CloudFront, drop query strings silently
    if ($args != "") {
        rewrite ^(.*)$ $1? last;
    }
    proxy_pass http://gunicorn;
}

location ~ "^/faq$" {
    limit_except GET HEAD { deny all; }
    # Public page - UTMs logged by CloudFront, drop query strings silently
    if ($args != "") {
        rewrite ^(.*)$ $1? last;
    }
    proxy_pass http://gunicorn;
}

location ~ "^/healthz$" {
    access_log off;
    limit_except GET HEAD { deny all; }
    # Public page - UTMs logged by CloudFront, drop query strings silently
    if ($args != "") {
        rewrite ^(.*)$ $1? last;
    }
    proxy_pass http://gunicorn;
}

location ~ "^/instructions$" {
    limit_except GET HEAD { deny all; }
    # Public page - UTMs logged by CloudFront, drop query strings silently
    if ($args != "") {
        rewrite ^(.*)$ $1? last;
    }
    proxy_pass http://gunicorn;
}

location ~ "^/llm\.txt$" {
    limit_except GET HEAD { deny all; }
    # Public page - UTMs logged by CloudFront, drop query strings silently
    if ($args != "") {
        rewrite ^(.*)$ $1? last;
    }
    proxy_pass http://gunicorn;
}

location ~ "^/login$" {
    limit_except GET HEAD { deny all; }
    # Public page - UTMs logged by CloudFront, drop query strings silently
    if ($args != "") {
        rewrite ^(.*)$ $1? last;
    }
    proxy_pass http://gunicorn;
}

location ~ "^/logout$" {
    limit_except GET HEAD { deny all; }
    # Public page - UTMs logged by CloudFront, drop query strings silently
    if ($args != "") {
        rewrite ^(.*)$ $1? last;
    }
    proxy_pass http://gunicorn;
}

location ~ "^/pricing$" {
    limit_except GET HEAD { deny all; }
    # Public page - UTMs logged by CloudFront, drop query strings silently
    if ($args != "") {
        rewrite ^(.*)$ $1? last;
    }
    proxy_pass http://gunicorn;
}

location ~ "^/privacy$" {
    limit_except GET HEAD { deny all; }
    # Public page - UTMs logged by CloudFront, drop query strings silently
    if ($args != "") {
        rewrite ^(.*)$ $1? last;
    }
    proxy_pass http://gunicorn;
}

location ~ "^/robots\.txt$" {
    limit_except GET HEAD { deny all; }
    # Public page - UTMs logged by CloudFront, drop query strings silently
    if ($args != "") {
        rewrite ^(.*)$ $1? last;
    }
    proxy_pass http://gunicorn;
}

location ~ "^/sitemap\.xml$" {
    limit_except GET HEAD { deny all; }
    # Public page - UTMs logged by CloudFront, drop query strings silently
    if ($args != "") {
        rewrite ^(.*)$ $1? last;
    }
    proxy_pass http://gunicorn;
}

location ~ "^/statement/[^/]+$" {
    proxy_read_timeout 120s;
    limit_except GET HEAD POST { deny all; }
    # Allowed parameters: items_view, show_payments, download
    if ($args ~ ".+" ) {set $check_args 1;}
    if ($args !~ "^(items_view=[^&<>\x22\x27]*|show_payments=[^&<>\x22\x27]*|download=[^&<>\x22\x27]*)(&(items_view=[^&<>\x22\x27]*|show_payments=[^&<>\x22\x27]*|download=[^&<>\x22\x27]*))*$") {
        set $check_args "${check_args}1";
    }
    if ($check_args = "11") {
        return 404;
    }
    proxy_pass http://gunicorn;
}

location ~ "^/statement/[^/]+/delete$" {
    limit_except POST { deny all; }
    # Private route - no query strings allowed
    if ($args != "") {
        rewrite ^(.*)$ $1? last;
    }
    proxy_pass http://gunicorn;
}

location ~ "^/statements$" {
    limit_except GET HEAD { deny all; }
    # Allowed parameters: view, sort, dir
    if ($args ~ ".+" ) {set $check_args 1;}
    if ($args !~ "^(view=[^&<>\x22\x27]*|sort=[^&<>\x22\x27]*|dir=[^&<>\x22\x27]*)(&(view=[^&<>\x22\x27]*|sort=[^&<>\x22\x27]*|dir=[^&<>\x22\x27]*))*$") {
        set $check_args "${check_args}1";
    }
    if ($check_args = "11") {
        return 404;
    }
    proxy_pass http://gunicorn;
}

location ~ "^/tenant_management$" {
    limit_except GET HEAD { deny all; }
    # Private route - no query strings allowed
    if ($args != "") {
        rewrite ^(.*)$ $1? last;
    }
    proxy_pass http://gunicorn;
}

location ~ "^/tenants/disconnect$" {
    limit_except POST { deny all; }
    # Private route - no query strings allowed
    if ($args != "") {
        rewrite ^(.*)$ $1? last;
    }
    proxy_pass http://gunicorn;
}

location ~ "^/tenants/select$" {
    limit_except POST { deny all; }
    # Private route - no query strings allowed
    if ($args != "") {
        rewrite ^(.*)$ $1? last;
    }
    proxy_pass http://gunicorn;
}

location ~ "^/terms$" {
    limit_except GET HEAD { deny all; }
    # Public page - UTMs logged by CloudFront, drop query strings silently
    if ($args != "") {
        rewrite ^(.*)$ $1? last;
    }
    proxy_pass http://gunicorn;
}

location ~ "^/upload-statements$" {
    client_max_body_size 10m;
    limit_except GET HEAD POST { deny all; }
    # Private route - no query strings allowed
    if ($args != "") {
        rewrite ^(.*)$ $1? last;
    }
    proxy_pass http://gunicorn;
}
