# Auto-generated Flask route location blocks
# Total routes: 29
# Container logging: Uses stderr for CloudWatch integration

# Routes with allowed query parameters:
#   /callback: error, error_description
#   /checkout/failed: ref
#   /checkout/success: session_id
#   /configs: contact_name
#   /statement/<statement_id>: items_view, show_payments, download
#   /statements: view, sort, dir

# Routes with directive overrides:
#   /api/upload-statements/preflight: client_max_body_size=10m
#   /upload-statements: client_max_body_size=10m

location ~ "^/$" {
    access_log off;
    limit_except GET HEAD { deny all; }
    if ($request_method = HEAD) {return 200;}
    # Public page - UTMs logged by CloudFront, drop query strings silently
    if ($args != "") {
        rewrite ^(.*)$ $1? last;
    }
    proxy_pass http://gunicorn;
}

location ~ "^/about$" {
    access_log off;
    limit_except GET HEAD { deny all; }
    if ($request_method = HEAD) {return 200;}
    # Public page - UTMs logged by CloudFront, drop query strings silently
    if ($args != "") {
        rewrite ^(.*)$ $1? last;
    }
    proxy_pass http://gunicorn;
}

location ~ "^/api/banner/dismiss$" {
    access_log off;
    limit_except POST { deny all; }
    if ($request_method = HEAD) {return 200;}
    # Private route - no query strings allowed
    if ($args != "") {
        rewrite ^(.*)$ $1? last;
    }
    proxy_pass http://gunicorn;
}

location ~ "^/api/checkout/create$" {
    access_log off;
    limit_except POST { deny all; }
    if ($request_method = HEAD) {return 200;}
    # Private route - no query strings allowed
    if ($args != "") {
        rewrite ^(.*)$ $1? last;
    }
    proxy_pass http://gunicorn;
}

location ~ "^/api/configs/confirm$" {
    access_log off;
    limit_except POST { deny all; }
    if ($request_method = HEAD) {return 200;}
    # Private route - no query strings allowed
    if ($args != "") {
        rewrite ^(.*)$ $1? last;
    }
    proxy_pass http://gunicorn;
}

location ~ "^/api/configs/confirm-all$" {
    access_log off;
    limit_except POST { deny all; }
    if ($request_method = HEAD) {return 200;}
    # Private route - no query strings allowed
    if ($args != "") {
        rewrite ^(.*)$ $1? last;
    }
    proxy_pass http://gunicorn;
}

location ~ "^/api/tenant-statuses$" {
    access_log off;
    limit_except GET HEAD { deny all; }
    if ($request_method = HEAD) {return 200;}
    # Private route - no query strings allowed
    if ($args != "") {
        rewrite ^(.*)$ $1? last;
    }
    proxy_pass http://gunicorn;
}

location ~ "^/api/tenants/[^/]+/sync$" {
    access_log off;
    limit_except POST { deny all; }
    if ($request_method = HEAD) {return 200;}
    # Private route - no query strings allowed
    if ($args != "") {
        rewrite ^(.*)$ $1? last;
    }
    proxy_pass http://gunicorn;
}

location ~ "^/api/upload-statements/preflight$" {
    access_log off;
    client_max_body_size 10m;
    limit_except POST { deny all; }
    if ($request_method = HEAD) {return 200;}
    # Private route - no query strings allowed
    if ($args != "") {
        rewrite ^(.*)$ $1? last;
    }
    proxy_pass http://gunicorn;
}

location ~ "^/billing-details$" {
    access_log off;
    limit_except GET HEAD { deny all; }
    if ($request_method = HEAD) {return 200;}
    # Private route - no query strings allowed
    if ($args != "") {
        rewrite ^(.*)$ $1? last;
    }
    proxy_pass http://gunicorn;
}

location ~ "^/buy-tokens$" {
    access_log off;
    limit_except GET HEAD POST { deny all; }
    if ($request_method = HEAD) {return 200;}
    # Private route - no query strings allowed
    if ($args != "") {
        rewrite ^(.*)$ $1? last;
    }
    proxy_pass http://gunicorn;
}

location ~ "^/callback$" {
    access_log off;
    limit_except GET HEAD { deny all; }
    if ($request_method = HEAD) {return 200;}
    # Allowed parameters: error, error_description
    if ($args ~ ".+" ) {set $check_args 1;}
    if ($args !~ "^(error=[^&<>\x22\x27]*|error_description=[^&<>\x22\x27]*)(&(error=[^&<>\x22\x27]*|error_description=[^&<>\x22\x27]*))*$") {
        set $check_args "${check_args}1";
    }
    if ($check_args = "11") {
        return 404;
    }
    proxy_pass http://gunicorn;
}

location ~ "^/checkout/cancel$" {
    access_log off;
    limit_except GET HEAD { deny all; }
    if ($request_method = HEAD) {return 200;}
    # Private route - no query strings allowed
    if ($args != "") {
        rewrite ^(.*)$ $1? last;
    }
    proxy_pass http://gunicorn;
}

location ~ "^/checkout/failed$" {
    access_log off;
    limit_except GET HEAD { deny all; }
    if ($request_method = HEAD) {return 200;}
    # Allowed parameters: ref
    if ($args ~ ".+" ) {set $check_args 1;}
    if ($args !~ "^ref=[^&<>\x22\x27]*$") {
        set $check_args "${check_args}1";
    }
    if ($check_args = "11") {
        return 404;
    }
    proxy_pass http://gunicorn;
}

location ~ "^/checkout/success$" {
    access_log off;
    limit_except GET HEAD { deny all; }
    if ($request_method = HEAD) {return 200;}
    # Allowed parameters: session_id
    if ($args ~ ".+" ) {set $check_args 1;}
    if ($args !~ "^session_id=[^&<>\x22\x27]*$") {
        set $check_args "${check_args}1";
    }
    if ($check_args = "11") {
        return 404;
    }
    proxy_pass http://gunicorn;
}

location ~ "^/configs$" {
    access_log off;
    limit_except GET HEAD POST { deny all; }
    if ($request_method = HEAD) {return 200;}
    # Allowed parameters: contact_name
    if ($args ~ ".+" ) {set $check_args 1;}
    if ($args !~ "^contact_name=[^&<>\x22\x27]*$") {
        set $check_args "${check_args}1";
    }
    if ($check_args = "11") {
        return 404;
    }
    proxy_pass http://gunicorn;
}

location ~ "^/cookies$" {
    access_log off;
    limit_except GET HEAD { deny all; }
    if ($request_method = HEAD) {return 200;}
    # Public page - UTMs logged by CloudFront, drop query strings silently
    if ($args != "") {
        rewrite ^(.*)$ $1? last;
    }
    proxy_pass http://gunicorn;
}

location ~ "^/healthz$" {
    access_log off;
    limit_except GET HEAD { deny all; }
    if ($request_method = HEAD) {return 200;}
    # Private route - no query strings allowed
    if ($args != "") {
        rewrite ^(.*)$ $1? last;
    }
    proxy_pass http://gunicorn;
}

location ~ "^/instructions$" {
    access_log off;
    limit_except GET HEAD { deny all; }
    if ($request_method = HEAD) {return 200;}
    # Public page - UTMs logged by CloudFront, drop query strings silently
    if ($args != "") {
        rewrite ^(.*)$ $1? last;
    }
    proxy_pass http://gunicorn;
}

location ~ "^/login$" {
    access_log off;
    limit_except GET HEAD { deny all; }
    if ($request_method = HEAD) {return 200;}
    # Private route - no query strings allowed
    if ($args != "") {
        rewrite ^(.*)$ $1? last;
    }
    proxy_pass http://gunicorn;
}

location ~ "^/logout$" {
    access_log off;
    limit_except GET HEAD { deny all; }
    if ($request_method = HEAD) {return 200;}
    # Private route - no query strings allowed
    if ($args != "") {
        rewrite ^(.*)$ $1? last;
    }
    proxy_pass http://gunicorn;
}

location ~ "^/pricing$" {
    access_log off;
    limit_except GET HEAD { deny all; }
    if ($request_method = HEAD) {return 200;}
    # Public page - UTMs logged by CloudFront, drop query strings silently
    if ($args != "") {
        rewrite ^(.*)$ $1? last;
    }
    proxy_pass http://gunicorn;
}

location ~ "^/statement/[^/]+$" {
    access_log off;
    limit_except GET HEAD POST { deny all; }
    if ($request_method = HEAD) {return 200;}
    # Allowed parameters: items_view, show_payments, download
    if ($args ~ ".+" ) {set $check_args 1;}
    if ($args !~ "^(items_view=[^&<>\x22\x27]*|show_payments=[^&<>\x22\x27]*|download=[^&<>\x22\x27]*)(&(items_view=[^&<>\x22\x27]*|show_payments=[^&<>\x22\x27]*|download=[^&<>\x22\x27]*))*$") {
        set $check_args "${check_args}1";
    }
    if ($check_args = "11") {
        return 404;
    }
    proxy_pass http://gunicorn;
}

location ~ "^/statement/[^/]+/delete$" {
    access_log off;
    limit_except POST { deny all; }
    if ($request_method = HEAD) {return 200;}
    # Private route - no query strings allowed
    if ($args != "") {
        rewrite ^(.*)$ $1? last;
    }
    proxy_pass http://gunicorn;
}

location ~ "^/statements$" {
    access_log off;
    limit_except GET HEAD { deny all; }
    if ($request_method = HEAD) {return 200;}
    # Allowed parameters: view, sort, dir
    if ($args ~ ".+" ) {set $check_args 1;}
    if ($args !~ "^(view=[^&<>\x22\x27]*|sort=[^&<>\x22\x27]*|dir=[^&<>\x22\x27]*)(&(view=[^&<>\x22\x27]*|sort=[^&<>\x22\x27]*|dir=[^&<>\x22\x27]*))*$") {
        set $check_args "${check_args}1";
    }
    if ($check_args = "11") {
        return 404;
    }
    proxy_pass http://gunicorn;
}

location ~ "^/tenant_management$" {
    access_log off;
    limit_except GET HEAD { deny all; }
    if ($request_method = HEAD) {return 200;}
    # Private route - no query strings allowed
    if ($args != "") {
        rewrite ^(.*)$ $1? last;
    }
    proxy_pass http://gunicorn;
}

location ~ "^/tenants/disconnect$" {
    access_log off;
    limit_except POST { deny all; }
    if ($request_method = HEAD) {return 200;}
    # Private route - no query strings allowed
    if ($args != "") {
        rewrite ^(.*)$ $1? last;
    }
    proxy_pass http://gunicorn;
}

location ~ "^/tenants/select$" {
    access_log off;
    limit_except POST { deny all; }
    if ($request_method = HEAD) {return 200;}
    # Private route - no query strings allowed
    if ($args != "") {
        rewrite ^(.*)$ $1? last;
    }
    proxy_pass http://gunicorn;
}

location ~ "^/upload-statements$" {
    access_log off;
    client_max_body_size 10m;
    limit_except GET HEAD POST { deny all; }
    if ($request_method = HEAD) {return 200;}
    # Private route - no query strings allowed
    if ($args != "") {
        rewrite ^(.*)$ $1? last;
    }
    proxy_pass http://gunicorn;
}
